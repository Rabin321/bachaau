import 'package:flutter/material.dart';

class SamplePolice extends StatelessWidget {
  const SamplePolice({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
